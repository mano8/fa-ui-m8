version: 2

updates:
  # GitHub Actions — keep workflow action SHAs current
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "ci"

  # npm — Astro / React front-end dependencies
  - package-ecosystem: "npm"
    directory: "/app"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "deps"

  # npm — the runtime image's static file server (B38-static-server-lock)
  - package-ecosystem: "npm"
    directory: "/docker/static-server"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "deps"

  # Docker — front-end image base
  - package-ecosystem: "docker"
    directory: "/docker"
    schedule:
      interval: "monthly"
    commit-message:
      prefix: "deps"
